# 変更履歴

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [1.2.0] - 2026-09-05

ユーザー視点での総点検にもとづく、不具合修正と操作性の改善です。

### 修正
- **PiP が二度と操作できなくなる状態を塞ぎました** — 不透明度を通常時・マウスオーバー時とも 0%
  にし、クリックスルーを有効にし、PiP切替ホットキーを未設定にすると、ウィンドウが見えず・
  触れず・呼び戻せなくなり、しかもその状態が保存されるため再起動しても復旧できませんでした。
  マウスオーバー時の不透明度に下限 10% を設け、クリックスルーは PiP切替ホットキーが
  有効なときだけ使えるようにしました
- **連写中に保存が失敗すると、モーダルダイアログが無限に出続ける問題を修正しました** —
  ディスク満杯や保存先ドライブの取り外しで発生していました。最初の失敗で連写を停止します
- **スナップショットのたびに UI が 4 秒固まる場合がある問題を修正しました** — 機器が静止画を
  返さない場合、毎回タイムアウトを待っていました。待ち時間を 1.5 秒に短縮し、2回続けて
  失敗したら従来方式に自動で切り替えます
- **オフライン時に「更新はありません」と表示される問題を修正しました** — 確認の失敗と
  「最新である」ことを区別していませんでした
- 「更新を確認」を連打すると、確認・ダウンロードが多重に走る問題を修正しました
- 更新確認中にウィンドウを閉じると例外が出る問題を修正しました
- **設定ファイルの保存を不可分（アトミック）にしました** — 書き込み中の電源断で
  settings.json が壊れ、次回起動時に全設定が黙って初期値へ戻っていました。
  読み込めなかった場合は `settings.bad.json` として保全し、その旨を通知します
- PiP のサイズ 10%〜20% が最小ウィンドウサイズに阻まれ、正しく小さくならない問題を修正しました
- ウィンドウサイズのプリセットが画面外にはみ出す問題を修正しました（4K入力で200%など）
- Windows起動時の自動実行が、exe を移動した後も「登録済み」と表示される問題を修正しました
- 保存先フォルダが使えない場合（USBの取り外しなど）に、既定のフォルダへ退避するようにしました
- 一時停止（Space）が画面のコピーではなく入力そのものを保持するようになりました。
  他のウィンドウが重なっていても、半透明のPiP中でも、正しい静止画になります

### 変更
- **起動時にデバイスの初期化を待たなくなりました** — 従来はウィンドウが出るまで最大10秒
  かかることがあり、「起動していない」ように見えていました
- **デバイス未接続時のモーダルダイアログを廃止しました** — 映像と音声で同じことを2回
  聞かれていました。接続すれば自動で再接続するため、画面上の案内に変えました
- **エラー表示を人間向けの文章にしました** — `Exception from HRESULT: 0x80070005` のような
  内部情報ではなく、「他のアプリが使用中の可能性があります」などと表示します。
  技術的な詳細は従来どおり error.log に記録されます
- **OSD の表示時間を用途に応じて延ばしました** — 「保存しました（クリックで開く）」が
  クリックする前に消えていました
- 「入力レベルを最大にする」は確認してから実行するようになりました。Windows 全体の
  録音音量を変更し他アプリにも影響するためです。「入力レベルを元に戻す」を追加しました
- メニューの表記を統一しました（メニューバーと右クリックメニューで名称と
  チェックの意味が食い違っていました）
- PiP のメニューに表示されるホットキーが、設定した実際のキーになりました
- 高DPI環境（125〜200%）で各ダイアログのレイアウトが崩れないようにしました
- ステータスバーのデバイス名を省略表示し、全文はマウスオーバーで表示するようにしました
- 補足文のコントラストを読みやすい水準に上げました
- 英語UIで日本語のまま表示されていた箇所（ウィンドウタイトル、`--help`、
  更新ダイアログの見出し、エラー文など）を翻訳しました
- ホットキー設定で、同じキーを複数の操作に割り当てられないようにしました。
  Enter キーでも決定できるようになりました
- error.log が無制限に増えないよう、1MB でローテーションするようにしました
- `--volume` の指定範囲を実際の上限（0〜500）に合わせました

### セキュリティ
- **更新の通信先を GitHub のホストに限定しました** — 従来は settings.json に書けば
  任意のホストを指定でき、そのホストが配布物と SHA256 の両方を返せるため、
  ハッシュ検証が意味を失う状態でした
- リリースページを開く際に、http/https 以外のURLを実行しないようにしました

## [1.1.0] - 2026-08-27

### 追加
- **スナップショットがソース解像度になりました** — Media Foundation のフォトsinkから
  取得するようになり、ウィンドウが小さくても入力解像度のまま保存されます。
  他のウィンドウが重なっていても影響を受けません（従来は画面のコピーでした）。
  フォトsinkが使えない機器では従来方式に自動で切り替わります
- 保存通知（OSD）をクリックすると、そのファイルをエクスプローラーで開けるようになりました
- 更新の通知に「この版をスキップ」を追加しました。断った版は次のリリースまで再通知しません
- 更新後の初回起動で、新しいバージョンになったことを通知するようにしました
- 音量を ↑ / ↓ キーで変更できるようにしました
- 2つ目を起動したときに、既存のウィンドウを前面に出すようにしました。
  コマンドライン引数（`--fullscreen` など）も既存のインスタンスへ引き継がれます
- 連写の実行中は、メニューの表記が「連写を停止 (3/10)」のように変わるようになりました
- タグを push すると GitHub Actions がビルドからリリース作成までを自動で行うようになりました

### 変更
- 設定を変更の数秒後に自動保存するようにしました（従来は終了時のみで、
  クラッシュするとその回の変更が失われていました）
- ホットキーの競合通知を、同じ内容については1回だけ表示するようにしました
- クリックスルーを有効にしたとき、解除に使うキーを実際の設定値で案内するようにしました

### 修正
- 保存先フォルダを開く操作が、パスに空白を含む場合に失敗する問題を修正しました
- 更新確認先が HTTPS でない場合は通信しないようにしました

## [1.0.3] - 2026-08-23

### 修正
- 更新時にダウンロードした一時ファイル（約3MB）が削除されず残り続ける問題を修正しました

## [1.0.2] - 2026-08-23

### 変更
- 更新の確認を起動のたびに実行するようにしました（従来は1日1回まで）

## [1.0.1] - 2026-08-22

### 追加
- **自動更新** — 新しいバージョンを検出したら、確認のうえダウンロード・置換・再起動まで
  自動で行います。SHA256 で検証します
- カーソル自動非表示（時間設定可）
- 音量を最大500%まで（ソフトリミッター内蔵）、入力レベルの最大化

### 変更
- 音声バッファの設定値を、実際に維持する遅延の目標値として扱うようにしました。
  低遅延／標準／安定のプリセットを追加しました
- LGPL 依存（DirectShowLib）を削除し、依存を MIT のみに整理しました

## [1.0.0] - 2026-08-08

初回リリース。

[1.2.0]: https://github.com/Yu5rin/YuCap/releases/tag/v1.2.0
[1.1.0]: https://github.com/Yu5rin/YuCap/releases/tag/v1.1.0
[1.0.3]: https://github.com/Yu5rin/YuCap/releases/tag/v1.0.3
[1.0.2]: https://github.com/Yu5rin/YuCap/releases/tag/v1.0.2
[1.0.1]: https://github.com/Yu5rin/YuCap/releases/tag/v1.0.1
[1.0.0]: https://github.com/Yu5rin/YuCap/releases/tag/v1.0.0
